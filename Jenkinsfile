pipeline {
    agent any
    
        stages {
        stage('run') {
            steps {
                echo 'This is run stepppp'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        
    }
    }
}